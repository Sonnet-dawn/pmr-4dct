# Tested on Python 3.9.25 with the versions below; upper bounds are deliberate,
# so that a fresh install years from now does not silently pick up a new major
# version of a dependency. See docs/verification.md for the checked environment.
torch>=2.0,<3.0
SimpleITK>=2.2,<3.0
numpy>=1.24,<3.0
scipy>=1.10,<2.0
